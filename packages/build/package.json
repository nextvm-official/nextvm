{
	"name": "@nextvm/build",
	"version": "0.0.1",
	"description": "NextVM build orchestrator — project loader, tsup pipeline, fxmanifest generation, dev mode",
	"license": "LGPL-3.0",
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/nextvm-official/nextvm.git",
		"directory": "packages/build"
	},
	"homepage": "https://github.com/nextvm-official/nextvm/tree/main/packages/build#readme",
	"bugs": "https://github.com/nextvm-official/nextvm/issues",
	"engines": {
		"node": ">=22.0.0 <23.0.0"
	},
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"exports": {
		".": {
			"import": "./dist/index.js",
			"types": "./dist/index.d.ts"
		}
	},
	"scripts": {
		"build": "tsup",
		"dev": "tsup --watch",
		"typecheck": "tsc --noEmit",
		"test": "vitest run --pool=forks --poolOptions.forks.singleFork=true",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@nextvm/core": "workspace:*",
		"chokidar": "^4.0.0",
		"jiti": "^2.4.0",
		"picocolors": "^1.1.0",
		"tsup": "^8.4.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@nextvm/test-utils": "workspace:*",
		"@types/node": "^22.0.0",
		"typescript": "^5.7.0",
		"vitest": "^3.0.0"
	}
}
